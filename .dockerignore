.git
.github
.vscode
.devcontainer
node_modules
.svelte-kit
build
.env
.env.*
# The schema must reach the image — varlock validates against it at build and
# at container start. It holds no secrets.
!.env.schema
!.env.example
Dockerfile*
