node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env — .env.schema is the committed source of truth (varlock); values stay local.
.env
.env.*
!.env.schema
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
test-results
/playwright/.cache

# SQLite
*.db

# Logs
logs
*.log
npm-debug.log*

# Coverage
coverage

# pnpm
.pnpm-store

# Editors
.vscode/
.idea/

# Claude Code local settings
.claude/settings.local.json

# Sentry Config File
.env.sentry-build-plugin

# Playwright — reports, traces and downloaded browsers are all reproducible output
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Review screenshots. Every one of these is produced by a throwaway Playwright
# spec so a change can be looked at rather than described, and regenerated on
# demand — none of them is a fixture anything depends on.
*-shots/
